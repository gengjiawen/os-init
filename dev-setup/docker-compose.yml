version: '3'

services:
  node-core-dev:
    build: ./
    container_name: 'node-dev'
    restart: always
    tty: true
    ports:
      - '2222:22'
    cap_add:
      - SYS_PTRACE
    volumes:
      - .:/pwd
      - $HOME/.local:/root/.local
